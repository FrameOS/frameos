import { MakeLogicType, kea, key, path, props } from 'kea'

import { loaders } from 'kea-loaders'
import { apiFetch } from '../../../../utils/apiFetch'
import { downloadZip } from '../../../../utils/downloadJson'
import type { FrameId } from '../../../../types'

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface frameSettingsLogicValues {
  binaryZip: boolean
  binaryZipLoading: boolean
  buildCache: boolean
  buildCacheLoading: boolean
  buildZip: boolean
  buildZipLoading: boolean
  cSourceZip: boolean
  cSourceZipLoading: boolean
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface frameSettingsLogicActions {
  clearBuildCache: () => any
  clearBuildCacheFailure: (
    error: string,
    errorObject?: any
  ) => {
    error: string
    errorObject?: any
  }
  clearBuildCacheSuccess: (
    buildCache: boolean,
    payload?: any
  ) => {
    buildCache: boolean
    payload?: any
  }
  downloadBinaryZip: () => any
  downloadBinaryZipFailure: (
    error: string,
    errorObject?: any
  ) => {
    error: string
    errorObject?: any
  }
  downloadBinaryZipSuccess: (
    binaryZip: boolean,
    payload?: any
  ) => {
    binaryZip: boolean
    payload?: any
  }
  downloadBuildZip: () => any
  downloadBuildZipFailure: (
    error: string,
    errorObject?: any
  ) => {
    error: string
    errorObject?: any
  }
  downloadBuildZipSuccess: (
    buildZip: boolean,
    payload?: any
  ) => {
    buildZip: boolean
    payload?: any
  }
  downloadCSourceZip: () => any
  downloadCSourceZipFailure: (
    error: string,
    errorObject?: any
  ) => {
    error: string
    errorObject?: any
  }
  downloadCSourceZipSuccess: (
    cSourceZip: boolean,
    payload?: any
  ) => {
    cSourceZip: boolean
    payload?: any
  }
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface frameSettingsLogicProps {
  frameId: FrameId
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface frameSettingsLogicMeta {
  key: FrameId
}

export type frameSettingsLogicType = MakeLogicType<
  frameSettingsLogicValues,
  frameSettingsLogicActions,
  frameSettingsLogicProps
> &
  frameSettingsLogicMeta

export const frameSettingsLogic = kea<frameSettingsLogicType>([
  path(['src', 'scenes', 'frame', 'panels', 'FrameSettings', 'frameSettingsLogic']),
  props({} as { frameId: FrameId }),
  key((props) => props.frameId),
  loaders(({ props }) => ({
    buildCache: [
      false,
      {
        clearBuildCache: async () => {
          if (confirm('Are you sure you want to clear the build cache?')) {
            try {
              await apiFetch(`/api/frames/${props.frameId}/clear_build_cache`, { method: 'POST' })
            } catch (error) {
              console.error(error)
            }
          }
          return false
        },
      },
    ],
    buildZip: [
      false,
      {
        downloadBuildZip: async () => {
          const response = await apiFetch(`/api/frames/${props.frameId}/download_build_zip`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/zip' },
          })
          if (!response.ok) {
            throw new Error('Failed to download build zip')
          }
          downloadZip(await response.blob(), `frame_${props.frameId}_build.zip`)
          return false
        },
      },
    ],
    cSourceZip: [
      false,
      {
        downloadCSourceZip: async () => {
          const response = await apiFetch(`/api/frames/${props.frameId}/download_c_source_zip`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/zip' },
          })
          if (!response.ok) {
            throw new Error('Failed to generate C sources .zip')
          }
          downloadZip(await response.blob(), `frame_${props.frameId}_c_source.zip`)
          return false
        },
      },
    ],
    binaryZip: [
      false,
      {
        downloadBinaryZip: async () => {
          const response = await apiFetch(`/api/frames/${props.frameId}/download_binary_zip`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/zip' },
          })
          if (!response.ok) {
            throw new Error('Failed to download built binary .zip')
          }
          downloadZip(await response.blob(), `frame_${props.frameId}_binary.zip`)
          return false
        },
      },
    ],
  })),
])
