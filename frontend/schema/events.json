[
  {
    "name": "init",
    "description": "Once per scene, when it is first loaded and its apps are ready — before its first open and render.",
    "canListen": true,
    "fields": []
  },
  {
    "name": "render",
    "description": "Render the scene. Triggered according to the frame's refresh interval, or when explicitly requested.",
    "canListen": true,
    "canDispatch": true,
    "fields": []
  },
  {
    "name": "open",
    "description": "Every time the scene becomes the frame's current scene: at boot, and whenever the frame switches to it, including switching back.",
    "canListen": true,
    "fields": [
      {
        "name": "sceneId",
        "label": "Scene",
        "type": "scene",
        "required": true
      }
    ]
  },
  {
    "name": "close",
    "description": "When the frame switches away from the scene to another one. Carries no payload.",
    "canListen": true,
    "fields": []
  },
  {
    "name": "keyDown",
    "description": "When a key is pressed on a keyboard",
    "canListen": true,
    "fields": [
      {
        "name": "key",
        "label": "Key",
        "type": "string",
        "required": true
      },
      {
        "name": "code",
        "label": "Code",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "keyUp",
    "description": "When a key is released on a keyboard",
    "canListen": true,
    "fields": [
      {
        "name": "key",
        "label": "Key",
        "type": "string",
        "required": true
      },
      {
        "name": "code",
        "label": "Code",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "mouseMove",
    "description": "When a mouse moves (not logged)",
    "canListen": true,
    "fields": [
      {
        "name": "x",
        "label": "X",
        "type": "integer",
        "required": true
      },
      {
        "name": "y",
        "label": "Y",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "mouseDown",
    "description": "When a mouse button is pressed (not logged)",
    "canListen": true,
    "fields": [
      {
        "name": "button",
        "label": "Button",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "mouseUp",
    "description": "When a mouse button is released (not logged)",
    "canListen": true,
    "fields": [
      {
        "name": "button",
        "label": "Button",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "wheel",
    "description": "When a mouse wheel is scrolled (not logged). Deltas are wheel notches; deltaY is positive when scrolling down (towards the user), deltaX when scrolling right. Linux frames only.",
    "canListen": true,
    "fields": [
      {
        "name": "deltaX",
        "label": "Delta X",
        "type": "integer",
        "required": true
      },
      {
        "name": "deltaY",
        "label": "Delta Y",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "turnOn",
    "description": "Turn the screen on",
    "canListen": true,
    "canDispatch": true,
    "fields": []
  },
  {
    "name": "turnOff",
    "description": "Turn the screen off",
    "canListen": true,
    "canDispatch": true,
    "fields": []
  },
  {
    "name": "button",
    "description": "When a GPIO button is pressed",
    "canListen": true,
    "fields": [
      {
        "name": "pin",
        "label": "Pin",
        "type": "integer",
        "required": true
      },
      {
        "name": "label",
        "label": "Label",
        "type": "string",
        "required": true
      },
      {
        "name": "level",
        "label": "Level",
        "type": "integer",
        "required": true
      }
    ]
  },
  {
    "name": "setSceneState",
    "description": "Set the scene's public state. This is not used for private state changes.",
    "canDispatch": true,
    "fields": []
  },
  {
    "name": "setCurrentScene",
    "description": "Switch the frame to another scene. An optional `state` object is applied to that scene's public state.",
    "canDispatch": true,
    "fields": [
      {
        "name": "sceneId",
        "label": "Scene",
        "type": "scene",
        "required": true
      }
    ]
  }
]
