// Where a sign-in lands when nothing asked for a specific page: the frames
// workspace, the thing people sign in for. Importable from client
// components too (sign-in.ts is server-only).
export const defaultSignInRedirect = "/frames";
