# Runtime-only top-level backend dependencies for the Docker image.
# Keep this as a subset of requirements.in. Docker installs these packages at
# the versions AND hashes of requirements.txt (tools/hashed_subset.py), while
# Flox installs the full development lock.
aiofiles
aiomqtt
alembic
arq
asyncssh
cryptography
email_validator
fastapi[standard]
fonttools
httpx
modal>=1.4.0
openai
packaging
pillow
posthog>=7.5.1
python-jose
redis
requests
sqlalchemy
sqlmodel
werkzeug
