# This file is autogenerated

{.warning[UnusedImport]: off.}
import pixie, json, times, strformat, strutils, sequtils, options, algorithm
import std/monotimes

import frameos/values
import frameos/types
import frameos/channels
import frameos/utils/image
import frameos/utils/url
import frameos/utils/time
import apps/render/split/app as render_splitApp
import apps/data/clock/app as data_clockApp
import apps/render/text/app as render_textApp

const DEBUG = false
let PUBLIC_STATE_FIELDS*: seq[StateField] = @[]
let PERSISTED_STATE_KEYS*: seq[string] = @[]

type Scene* = ref object of FrameScene
  node1: render_splitApp.App
  node2: render_textApp.App
  node3: render_textApp.App
  node4: render_textApp.App
  node5: render_textApp.App
  node6: render_textApp.App
  node7: render_textApp.App
  node8: data_clockApp.App
  node9: data_clockApp.App
  node10: data_clockApp.App
  node11: data_clockApp.App
  node12: data_clockApp.App
  node13: data_clockApp.App

{.push hint[XDeclaredButNotUsed]: off.}


proc runNode*(self: Scene, nodeId: NodeId, context: ExecutionContext, asDataNode = false): Value =
  result = VNone()
  let scene = self
  let frameConfig = scene.frameConfig
  let state = scene.state
  var nextNode = nodeId
  var currentNode = nodeId
  var timer = epochTime()
  while nextNode != -1.NodeId:
    currentNode = nextNode
    timer = epochTime()
    case nextNode:
    of 1.NodeId: # render/split
      self.node1.run(context)
      nextNode = -1.NodeId
    of 2.NodeId: # render/text
      self.node2.appConfig.text = block:
        self.node8.get(context)
      self.node2.run(context)
      nextNode = -1.NodeId
    of 3.NodeId: # render/text
      self.node3.appConfig.text = block:
        self.node9.get(context)
      self.node3.run(context)
      nextNode = -1.NodeId
    of 4.NodeId: # render/text
      self.node4.appConfig.text = block:
        self.node10.get(context)
      self.node4.run(context)
      nextNode = -1.NodeId
    of 5.NodeId: # render/text
      self.node5.appConfig.text = block:
        self.node11.get(context)
      self.node5.run(context)
      nextNode = -1.NodeId
    of 6.NodeId: # render/text
      self.node6.appConfig.text = block:
        self.node12.get(context)
      self.node6.run(context)
      nextNode = -1.NodeId
    of 7.NodeId: # render/text
      self.node7.appConfig.text = block:
        self.node13.get(context)
      self.node7.run(context)
      nextNode = -1.NodeId
    else:
      nextNode = -1.NodeId
    
    if DEBUG:
      self.logger.log(%*{"event": "debug:scene", "node": currentNode, "ms": (-timer + epochTime()) * 1000})

proc runEvent*(self: Scene, context: ExecutionContext) =
  case context.event:
  of "render":
    context.image.fill(Scene(self).backgroundColor)
    try: discard self.runNode(1.NodeId, context)
    except Exception as e: self.logger.log(%*{"event": "render:error", "node": 1, "error": $e.msg, "stacktrace": e.getStackTrace()})
  of "setSceneState":
    if context.payload.hasKey("state") and context.payload["state"].kind == JObject:
      let payload = context.payload["state"]
      for field in PUBLIC_STATE_FIELDS:
        let key = field.name
        if payload.hasKey(key) and payload[key] != self.state{key}:
          self.state[key] = copy(payload[key])
    if context.payload.hasKey("render"):
      sendEvent("render", %*{})
  of "setCurrentScene":
    if context.payload.hasKey("state") and context.payload["state"].kind == JObject:
      let payload = context.payload["state"]
      for field in PUBLIC_STATE_FIELDS:
        let key = field.name
        if payload.hasKey(key) and payload[key] != self.state{key}:
          self.state[key] = copy(payload[key])
  else: discard

proc runEvent*(self: FrameScene, context: ExecutionContext) =
  runEvent(Scene(self), context)

proc render*(self: FrameScene, context: ExecutionContext): Image =
  runEvent(self, context)
  return context.image

proc init*(sceneId: SceneId, frameConfig: FrameConfig, logger: Logger, persistedState: JsonNode): FrameScene =
  var state = %*{}
  if persistedState.kind == JObject:
    for key in persistedState.keys:
      state[key] = persistedState[key]
  let scene = Scene(id: sceneId, frameConfig: frameConfig, state: state, logger: logger, refreshInterval: 3600.0, backgroundColor: parseHtmlColor("#000000"))
  let self = scene
  result = scene
  var context = ExecutionContext(scene: scene, event: "init", payload: state, hasImage: false, loopIndex: 0, loopKey: ".")
  scene.execNode = (proc(nodeId: NodeId, context: ExecutionContext) = discard scene.runNode(nodeId, context))
  scene.getDataNode = (proc(nodeId: NodeId, context: ExecutionContext): Value = scene.getDataNode(nodeId, context))
  scene.node1 = render_splitApp.App(nodeName: "render/split", nodeId: 1.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_splitApp.AppConfig(
    rows: 3,
    columns: 2,
    inputImage: none(Image),
    hideEmpty: false,
    render_functions: @[
      @[
        2.NodeId,
        3.NodeId,
      ],
      @[
        4.NodeId,
        5.NodeId,
      ],
      @[
        6.NodeId,
        7.NodeId,
      ],
    ],
    render_function: 0.NodeId,
  ))
  scene.node8 = data_clockApp.App(nodeName: "data/clock", nodeId: 8.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: data_clockApp.AppConfig(
    format: "HH:mm:ss",
    formatCustom: "",
  ))
  scene.node2 = render_textApp.App(nodeName: "render/text", nodeId: 2.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_textApp.AppConfig(
    fontSize: 20.0,
    borderWidth: 0,
    inputImage: none(Image),
    text: block:
      self.node8.get(context),
    richText: "disabled",
    position: "center",
    vAlign: "middle",
    offsetX: 0.0,
    offsetY: 0.0,
    padding: 10.0,
    fontColor: parseHtmlColor("#ffffff"),
    borderColor: parseHtmlColor("#000000"),
    overflow: "fit-bounds",
  ))
  scene.node9 = data_clockApp.App(nodeName: "data/clock", nodeId: 9.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: data_clockApp.AppConfig(
    format: "HH:mm",
    formatCustom: "",
  ))
  scene.node3 = render_textApp.App(nodeName: "render/text", nodeId: 3.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_textApp.AppConfig(
    fontSize: 20.0,
    borderWidth: 0,
    inputImage: none(Image),
    text: block:
      self.node9.get(context),
    richText: "disabled",
    position: "center",
    vAlign: "middle",
    offsetX: 0.0,
    offsetY: 0.0,
    padding: 10.0,
    fontColor: parseHtmlColor("#ffffff"),
    borderColor: parseHtmlColor("#000000"),
    overflow: "fit-bounds",
  ))
  scene.node10 = data_clockApp.App(nodeName: "data/clock", nodeId: 10.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: data_clockApp.AppConfig(
    format: "yyyy-MM-dd",
    formatCustom: "",
  ))
  scene.node4 = render_textApp.App(nodeName: "render/text", nodeId: 4.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_textApp.AppConfig(
    fontSize: 20.0,
    borderWidth: 0,
    inputImage: none(Image),
    text: block:
      self.node10.get(context),
    richText: "disabled",
    position: "center",
    vAlign: "middle",
    offsetX: 0.0,
    offsetY: 0.0,
    padding: 10.0,
    fontColor: parseHtmlColor("#ffffff"),
    borderColor: parseHtmlColor("#000000"),
    overflow: "fit-bounds",
  ))
  scene.node11 = data_clockApp.App(nodeName: "data/clock", nodeId: 11.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: data_clockApp.AppConfig(
    format: "yyyy-MM-dd HH:mm:ss",
    formatCustom: "",
  ))
  scene.node5 = render_textApp.App(nodeName: "render/text", nodeId: 5.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_textApp.AppConfig(
    fontSize: 20.0,
    borderWidth: 0,
    inputImage: none(Image),
    text: block:
      self.node11.get(context),
    richText: "disabled",
    position: "center",
    vAlign: "middle",
    offsetX: 0.0,
    offsetY: 0.0,
    padding: 10.0,
    fontColor: parseHtmlColor("#ffffff"),
    borderColor: parseHtmlColor("#000000"),
    overflow: "fit-bounds",
  ))
  scene.node12 = data_clockApp.App(nodeName: "data/clock", nodeId: 12.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: data_clockApp.AppConfig(
    format: "HH:mm:ss:fff",
    formatCustom: "",
  ))
  scene.node6 = render_textApp.App(nodeName: "render/text", nodeId: 6.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_textApp.AppConfig(
    fontSize: 20.0,
    borderWidth: 0,
    inputImage: none(Image),
    text: block:
      self.node12.get(context),
    richText: "disabled",
    position: "center",
    vAlign: "middle",
    offsetX: 0.0,
    offsetY: 0.0,
    padding: 10.0,
    fontColor: parseHtmlColor("#ffffff"),
    borderColor: parseHtmlColor("#000000"),
    overflow: "fit-bounds",
  ))
  scene.node13 = data_clockApp.App(nodeName: "data/clock", nodeId: 13.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: data_clockApp.AppConfig(
    format: "custom",
    formatCustom: "dddd d MMMM yyyy",
  ))
  scene.node7 = render_textApp.App(nodeName: "render/text", nodeId: 7.NodeId, scene: scene.FrameScene, frameConfig: scene.frameConfig, appConfig: render_textApp.AppConfig(
    fontSize: 20.0,
    borderWidth: 0,
    inputImage: none(Image),
    text: block:
      self.node13.get(context),
    richText: "disabled",
    position: "center",
    vAlign: "middle",
    offsetX: 0.0,
    offsetY: 0.0,
    padding: 10.0,
    fontColor: parseHtmlColor("#ffffff"),
    borderColor: parseHtmlColor("#000000"),
    overflow: "fit-bounds",
  ))
  runEvent(self, context)
  
{.pop.}

var exportedScene* = ExportedScene(
  publicStateFields: PUBLIC_STATE_FIELDS,
  persistedStateKeys: PERSISTED_STATE_KEYS,
  init: init,
  runEvent: runEvent,
  render: render
)
