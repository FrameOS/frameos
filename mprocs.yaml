procs:
  cloud:
    shell: pnpm run dev:cloud
  backend:
    shell: FRAMEOS_CLOUD_URL=http://localhost:3000 pnpm run dev:backend
  worker:
    shell: FRAMEOS_CLOUD_URL=http://localhost:3000 pnpm run dev:worker
  vite:
    shell: pnpm --dir frontend run dev:vite
  kea:
    shell: pnpm --dir frontend run dev:kea
  redis:
    shell: pnpm run dev:redis
  frameos:
    shell: cd frameos && make
    autostart: false
  backend-docker:
    shell: bash scripts/backend-docker.sh
    autostart: false
  migrate:
    shell: pnpm run dev:migrate
    autostart: false
